{
  "name": "Notifica Cambio Stato Preventivo",
  "description": "Invia email al cliente quando lo stato del preventivo cambia",
  "trigger": {
    "condition": "${ (.trigger.changed_fields | index(\"status\") != null) }",
    "config": {
      "trigger_type": "entity",
      "events": [
        "update"
      ],
      "entity_name": "Estimate"
    }
  },
  "definition": {
    "document": {
      "dsl": "1.0.0",
      "name": "notifica_cambio_stato_preventivo",
      "version": "1.0",
      "namespace": "base44"
    },
    "do": [
      {
        "run_function": {
          "call": "invoke_backend_function",
          "with": {
            "function_name": "notifyEstimateStatusChanged",
            "args": {}
          },
          "then": "end"
        }
      }
    ]
  },
  "x-base44-migrated-from-automation": {
    "legacy_payload_function": "notifyEstimateStatusChanged"
  }
}